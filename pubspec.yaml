name: mockup_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10
  google_fonts: ^6.1.0
  http: ^1.6.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  # google_sign_in: ^6.1.0        # ← ลด version ลง
  google_sign_in: ^6.2.1 
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  flutter_blue_plus: ^1.31.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - images/  
  uses-material-design: true